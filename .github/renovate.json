{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "timezone": "Europe/Madrid",
  "branchConcurrentLimit": 10,
  "prHourlyLimit": 10,
  "dependencyDashboard": true,
  "automerge": false,
  "automergeType": "pr",
  "automergeStrategy": "squash",
  "platformAutomerge": true,
  "dependencyDashboardOSVVulnerabilitySummary": "all",
  "commitMessagePrefix": "chore(deps):",
  "recreateWhen": "always",
  "ignorePaths": [
    ".github/renovate.json"
  ],
  "packageRules": [
    {
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "automerge": true
    },
    {
      "description": "The slsa-github-generator is the trust root of the L3 provenance claim (ADR 0014); it only accepts vX.Y.Z tags, so a human sees every bump.",
      "matchManagers": [
        "github-actions"
      ],
      "matchPackageNames": [
        "slsa-framework/slsa-github-generator"
      ],
      "automerge": false,
      "pinDigests": false
    },
    {
      "matchManagers": [
        "bazel"
      ],
      "postUpgradeTasks": {
        "commands": [
          "bazel mod deps --lockfile_mode=update",
          "bazel mod tidy"
        ],
        "fileFilters": [
          "MODULE.bazel.lock"
        ],
        "executionMode": "update"
      }
    },
    {
      "matchManagers": [
        "bazelisk"
      ],
      "postUpgradeTasks": {
        "commands": [
          "bazel mod deps --lockfile_mode=update",
          "bazel mod tidy"
        ],
        "fileFilters": [
          "MODULE.bazel.lock"
        ],
        "executionMode": "update"
      }
    },
    {
      "matchManagers": [
        "bazel-module"
      ],
      "postUpgradeTasks": {
        "commands": [
          "bazel mod deps --lockfile_mode=update",
          "bazel mod tidy"
        ],
        "fileFilters": [
          "MODULE.bazel.lock",
          "bazel/modules/*/MODULE.bazel.lock"
        ],
        "executionMode": "update"
      }
    },
    {
      "matchDepNames": [
        "go"
      ],
      "matchManagers": [
        "gomod"
      ],
      "rangeStrategy": "bump"
    },
    {
      "description": "Indirect Go deps are linked into the binaries we ship, so their CVEs fail the cve_tests (e.g. golang.org/x/crypto GO-2026-6354). Renovate disables indirect deps by default; grouped into one PR because go.mod carries hundreds of them.",
      "matchManagers": [
        "gomod"
      ],
      "matchDepTypes": [
        "indirect"
      ],
      "enabled": true,
      "groupName": "indirect Go dependencies",
      "groupSlug": "gomod-indirect"
    },
    {
      "description": "Digest bumps on pseudo-versions like v0.6.3-0.<date>-<hash> make Renovate write a bare commit hash where gazelle expects a semver, which breaks every post-upgrade bazel command. go mod tidy manages indirect pseudo-versions anyway.",
      "matchManagers": [
        "gomod"
      ],
      "matchDepTypes": [
        "indirect"
      ],
      "matchUpdateTypes": [
        "digest"
      ],
      "enabled": false
    },
    {
      "matchManagers": [
        "gomod"
      ],
      "postUpgradeTasks": {
        "commands": [
          "bazel run @rules_go//go -- mod tidy",
          "bazel mod deps --lockfile_mode=update",
          "bazel mod tidy"
        ],
        "fileFilters": [
          "go.mod",
          "go.sum",
          "bazel/include/go.MODULE.bazel",
          "MODULE.bazel.lock",
          "bazel/modules/*/MODULE.bazel",
          "bazel/modules/*/MODULE.bazel.lock"
        ],
        "executionMode": "branch"
      },
      "postUpdateOptions": [
        "goModTidy"
      ]
    },
    {
      "matchManagers": [
        "terraform",
        "terraform-lockfile"
      ],
      "postUpgradeTasks": {
        "commands": [
          "bazel mod deps --lockfile_mode=update",
          "bazel mod tidy"
        ],
        "fileFilters": [
          "infra/.terraform.lock.hcl",
          "MODULE.bazel.lock"
        ],
        "executionMode": "update"
      }
    },
    {
      "matchDepNames": [
        "rules_img",
        "rules_img_tool"
      ],
      "groupName": "rules_img",
      "groupSlug": "rules_img"
    },
    {
      "matchManagers": [
        "custom.regex"
      ],
      "matchFileNames": [
        "tools/tools.lock.json"
      ],
      "postUpgradeTasks": {
        "commands": [
          "./tools/repin --tool {{{depName}}}"
        ],
        "fileFilters": [
          "tools/tools.lock.json"
        ],
        "executionMode": "update"
      }
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "rules_multitool lockfile: tools published as GitHub Releases. The postUpgradeTask below re-runs multitool, which is what actually rewrites the URLs and sha256s for the bumped tool. kubectl and terraform are deliberately absent: multitool only understands GitHub Releases, so bumping their URLs here would leave a stale sha256 and break the build.",
      "managerFilePatterns": [
        "tools/tools.lock.json"
      ],
      "matchStrings": [
        "https://github\\.com/(?<packageScope>[^/\"]+)/(?<depName>[^/\"]+)/releases/download/(?<currentValue>v[^/\"]+)/"
      ],
      "packageNameTemplate": "{{{packageScope}}}/{{{depName}}}",
      "datasourceTemplate": "github-releases"
    }
  ]
}
