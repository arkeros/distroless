schemaVersion: 2.0.0
# The arch-specific half of test_hummingbird.yaml: on arm64 glibc puts the
# dynamic linker under /usr/lib/, not /usr/lib64/.
fileExistenceTests:
  - name: 'glibc dynamic linker'
    path: '/usr/lib/ld-linux-aarch64.so.1'
    shouldExist: true
